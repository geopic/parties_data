// -----------------------------------------------------------------------------
// Exported types, interfaces, etc for use across the application.
// -----------------------------------------------------------------------------
export interface IElements {
  [key: string]: Element|HTMLElement;
}

export interface IValues {
  [key: string]: string|number|boolean;
}
